if (false) {
  Metadata.find().forEach(function(record){
    if (record.schema && record.schema.allowedValues) {
      record.schema.allowedValues.forEach(function(allowedValue){

      });
    }
  });
};
